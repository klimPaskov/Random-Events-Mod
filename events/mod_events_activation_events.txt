add_namespace = mod_events.activation

country_event = {
	id = mod_events.activation.1
	title = mod_events.activation.1.t
	desc = mod_events.activation.1.d
	picture = GFX_report_event_generic_handshake
	
	is_triggered_only = yes
	
	option = {
		name = mod_events.activation.1.a
		set_variable = {
			var = global.days_timeout
			value = 15
		}
		activate_mission = mod_events_decision_main_timeout
		set_global_flag = events_activated
	}
	
	option = {
		name = mod_events.activation.1.b
		set_variable = {
			var = global.days_timeout
			value = 30
		}
		activate_mission = mod_events_decision_main_timeout
		set_global_flag = events_activated
	}
	
	option = {
		name = mod_events.activation.1.c
		set_variable = {
			var = global.days_timeout
			value = 60
		}
		activate_mission = mod_events_decision_main_timeout
		set_global_flag = events_activated
	}
}
# Multiplayer
country_event = {
	id = mod_events.activation.2
	title = mod_events.activation.2.t
	desc = mod_events.activation.2.d
	picture = GFX_report_event_generic_handshake
	
	is_triggered_only = yes
	
	option = {
		name = mod_events.activation.2.a
		set_variable = { global.event_receiver = ROOT.id }
		set_global_flag = event_receiver_chosen
	}
	
	option = {
		name = mod_events.activation.2.b
	}
}
# country_event = {
	# id = mod_events.activation.3
	# title = mod_events.activation.3.t
	# desc = mod_events.activation.3.d
	# picture = GFX_report_event_generic_handshake
	
	# fire_only_once = yes
	
	# trigger = {
		# date > 1936.1.2
		# NOT = { has_global_flag = event_receiver_chosen }
		# is_ai = no
	# }
	
	# mean_time_to_happen = {
		# days = 5
	# }
	
	# option = {
		# name = mod_events.activation.3.a
		# set_variable = { global.event_receiver = ROOT.id }
	# }
# }